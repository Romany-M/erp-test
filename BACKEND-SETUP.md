# المناهري ERP — بعد التحويل لـ Supabase (Backend حقيقي)

## اللي اتغيّر
النظام كان شغال محليًا بس (`localStorage`) — كل جهاز بياناته منفصلة، والدخول كان
باسورد ثابت (`admin/2020`) مكتوب صريح في الكود. دلوقتي:

- كل البيانات متخزنة في قاعدة بيانات Postgres حقيقية على Supabase، وبتتزامن
  فورًا بين أي جهاز يفتح الرابط.
- الدخول بقى حقيقي عبر Supabase Auth (بريد إلكتروني + كلمة مرور).
- كل جدول محمي بـ Row Level Security — البيانات مش قابلة للقراءة أو الكتابة
  إلا من مستخدم مسجّل دخول فعليًا.

## ⚠️ خطوة واحدة لازم تعملها بنفسك (دقيقة واحدة)
معنديش صلاحية إنشاء حساب المستخدم الأول برمجيًا (Supabase بيمنع الوصول
المباشر لجدول المستخدمين الداخلي لأسباب أمان). اعمل الآتي:

1. ادخل على: https://supabase.com/dashboard/project/ojslgytkohbcjncddkoj/auth/users
2. دوس **Add user** → **Create new user**
3. حط إيميل (مثلاً `admin@almanahry.com`) وكلمة مرور قوية من اختيارك
4. فعّل **Auto Confirm User** (مهم — من غيرها الحساب مش هيقدر يدخل)
5. احفظ

كده تقدر تدخل بنفس الإيميل والباسورد من صفحة تسجيل الدخول.

(تقدر تضيف حسابات تانية لموظفين تانيين بنفس الطريقة لاحقًا.)

## تشغيل المشروع محليًا
```bash
npm install
npm run dev
```
ملف `.env` موجود بالفعل جوه الملف المضغوط ومربوط بمشروع Supabase الحقيقي
(`almanahry-construction-system`)، فمش محتاج تعمل حاجة زيادة عشان تجربه محليًا.

## النشر على Vercel
لو بتنشر على Vercel، لازم تضيف نفس المتغيرين دول في إعدادات المشروع
(Project Settings → Environment Variables):

```
VITE_SUPABASE_URL=https://ojslgytkohbcjncddkoj.supabase.co
VITE_SUPABASE_ANON_KEY=<القيمة الموجودة في ملف .env عندك>
```

بعدها أي دومين تنشر عليه هيشتغل ويقرا/يكتب من نفس قاعدة البيانات.

## ملاحظة أمان
- الـ `anon key` دي مفتاح عام (public) وده طبيعي في Supabase — هي مش سر،
  الحماية الحقيقية جايه من RLS (كل جدول مقفول على `authenticated` بس).
- الباسورد اللي هتحطه في خطوة إنشاء المستخدم دي هي الوحيدة اللي لازم تفضل سرية.
- ينفع لاحقًا تضيف "نسيت كلمة المرور" أو حسابات بأدوار مختلفة (admin/محاسب)
  لو احتجت — الأساس (Supabase Auth) بيدعم ده جاهز.
